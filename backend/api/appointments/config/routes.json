{
  "routes": [
    {
      "method": "POST",
      "path": "/appointments/",
      "handler": "Appointments.createAppointment",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/appointments",
      "handler": "Appointments.getProfileAppointments",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/appointments/:id/cancel",
      "handler": "Appointments.cancelAppointment",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/appointments/terms",
      "handler": "Appointments.terms",
      "config": {
        "prefix": "",
        "tag": {
          "plugin": "appointments",
          "name": "Appointments",
          "actionType": "terms"
        }
      }
    },
    {
      "method": "GET",
      "path": "/appointments/contacts",
      "handler": "Appointments.contacts",
      "config": {
        "prefix": "",
        "tag": {
          "plugin": "appointments",
          "name": "Appointments",
          "actionType": "contacts"
        }
      }
    },
    {
      "method": "GET",
      "path": "/appointments/meta",
      "handler": "Appointments.meta",
      "config": {
        "prefix": "",
        "tag": {
          "plugin": "appointments",
          "name": "Appointments",
          "actionType": "meta"
        }
      }
    },
    {
      "method": "GET",
      "path": "/appointments/services",
      "handler": "Appointments.services",
      "config": {
        "policies": ["plugins.users-permissions.ratelimit"],
        "prefix": "",
        "tag": {
          "plugin": "appointments",
          "name": "Appointments",
          "actionType": "meta"
        }
      }
    },
    {
      "method": "GET",
      "path": "/appointments/employees",
      "handler": "Appointments.employees",
      "config": {
        "policies": ["plugins.users-permissions.ratelimit"],
        "prefix": "",
        "tag": {
          "plugin": "appointments",
          "name": "Appointments",
          "actionType": "meta"
        }
      }
    },
    {
      "method": "GET",
      "path": "/appointments/schedule/:id",
      "handler": "Appointments.schedule",
      "config": {
        "policies": ["plugins.users-permissions.ratelimit"],
        "prefix": "",
        "tag": {
          "plugin": "appointments",
          "name": "Appointments",
          "actionType": "meta"
        }
      }
    },
    {
      "method": "GET",
      "path": "/appointments/:id",
      "handler": "Appointments.getAppointment",
      "config": {
        "policies": []
      }
    }
  ]
}
